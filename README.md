# git_test

"Hello Odin!"
This project has proven really helpful

To acess the repository,
first clone the ssh link
then acess the folder, for this example the repository is called git_test so it is cd git_test inside the terminal